<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">


    <SwitchPreference
        android:defaultValue="false"
        android:key="pref_enabled_range_km"
        android:summary="@string/pref_description_km"
        android:title="@string/pref_title_km" />

    <!-- Range km form display POI -->
    <SeekBarPreference
        android:dependency="pref_enabled_range_km"
        android:defaultValue="20"
        android:max="100"
        android:title="Visualizza nel raggio di:"
        android:key="pref_range_km">
    </SeekBarPreference>


    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code.
    <EditTextPreference
        android:capitalize="words"
        android:defaultValue="@string/pref_default_display_name"
        android:inputType="textCapWords"
        android:key="example_text"
        android:maxLines="1"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:title="@string/pref_title_display_name"/>
         -->


</PreferenceScreen>
